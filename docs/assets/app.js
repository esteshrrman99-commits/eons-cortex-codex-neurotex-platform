const out=document.getElementById('output');const records={intake:['Create owner/entity placeholder','Attach source record locally','Assign evidence tier','Mark missing proof'],classify:['Tax year: placeholder','Entity: placeholder','Form/workflow: placeholder','Review required: yes'],gate:['No external action allowed','No unsupported credit claims','Treasury/IRS payment proof required','Professional review required'],review:['Generate checklist','Separate known facts from assumptions','Export binder draft','Human signoff required']};document.querySelectorAll('button[data-action]').forEach(b=>b.onclick=()=>{const a=b.dataset.action;out.textContent=`${a.toUpperCase()} CONTROL\n`+records[a].map((x,i)=>`${i+1}. ${x}`).join('\n')+`\n\nStatus: draft/private/local proof mode.`;});
