# EONS Backend Endpoint Map

Local backend:
- npm --prefix backend/vault-api start
- default port: 8787

Endpoints:
- GET /health
- GET /gate
- GET /audit-demo
- POST /upload-mock
- GET /encryption-gate
- POST /fingerprint-mock
- GET /auth-gate
- GET /session-mock?role=owner
- GET /schema
- GET /migration-gate
- GET /parser-gate
- POST /parse-mock
- GET /review-gate
- POST /review-note-mock
- GET /backup-gate
- POST /archive-manifest-mock
