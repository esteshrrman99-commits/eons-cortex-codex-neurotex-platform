# Backend Prompt 3 Local Test Notes

Adds scripts/test-vault-api.js.

Purpose: start the local mock vault API, test /health, /gate, and /audit-demo, then stop the server.

No sensitive uploads. No real encryption. No external submission.
